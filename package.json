{
  "name": "kaisen",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node --env-file-if-exists=.env server.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check brain.mjs && node --check providers.mjs && node --check scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
